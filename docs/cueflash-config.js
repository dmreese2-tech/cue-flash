// Points the GitHub Pages-hosted control.html / display.html at the actual
// Codespace running the server. Update this one file when the Codespace
// URL changes (e.g. after recreating the Codespace) — control.html and
// display.html both read from here, so there's nothing else to edit.
window.CUEFLASH_API_BASE = "https://obscure-spoon-vppv67p7xv7r2665q-3000.app.github.dev".replace(/\/+$/, '');
